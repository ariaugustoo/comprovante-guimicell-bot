services:
  - type: web
    name: dbhcomprovantebot
    env: python
    plan: free
    region: oregon
    buildCommand: |
      apt-get update && \
      apt-get install -y tesseract-ocr libtesseract-dev poppler-utils && \
      pip install -r requirements.txt
    startCommand: python main.py
