services:
  - type: web
    name: comprovante-guimicell-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "./start.sh"
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
